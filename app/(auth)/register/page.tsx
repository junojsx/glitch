import React from "react";

const RegisterPage = () => {
 return <div>RegisterPage</div>;
};

export default RegisterPage;

//by using the parentheses on the auth folder, it tells the router to not the name of the folder as part of the URL.
